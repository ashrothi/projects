package org.streaming.execute.process

import java.sql.Connection

import org.streaming.rule.config.Configuration

/*
 * While executing this object,tables will be created in Hsql by reading commands from Config File and will get the data from stored procedure and then create dynamic rule queries for the same and then process them.
 */
object ExecuteProcess {

  /*
   * Method is used to get data from stored procedure and create dynamic queries for the same.  
   */
  def executeProcess: Boolean = {
    
   
    
    
    return false;
  
  }
}